# Rendered by Ansible (harmony role). Secrets resolved from 1Password via op run.
# POLARIS_TSIG_SECRET is the DDNS key shared with polaris-auth, used by lego for
# the DNS-01 challenge.
POLARIS_TSIG_SECRET={{ lookup('env', 'POLARIS_TSIG_SECRET') }}
